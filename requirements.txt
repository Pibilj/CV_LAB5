# Core scientific computing
numpy>=2.0.0
matplotlib>=3.8.0
scipy>=1.11.0

# Machine Learning
scikit-learn>=1.3.0
scikit-image>=0.21.0

# Deep Learning (PyTorch)
# CUDA 12.8 support - will automatically use CPU if no GPU available
torch>=2.0.0
torchvision>=0.15.0
# For CUDA 12.8 support, install with:
# pip install torch torchvision --index-url https://download.pytorch.org/whl/cu128
# For CPU-only (smaller install), use:
# pip install torch torchvision --index-url https://download.pytorch.org/whl/cpu

# Data visualization
seaborn>=0.13.0

# Image processing
pillow>=10.0.0

# Utilities
tqdm>=4.65.0
